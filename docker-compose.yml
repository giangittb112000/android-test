services:
  android:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: android-test
    privileged: true

    devices:
      - /dev/kvm:/dev/kvm

    expose:
      - "6080"
      - "5555"

    environment:
      EMULATOR_DEVICE: "Samsung Galaxy S10"
      WEB_VNC: "true"
